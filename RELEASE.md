# نشر NAYVO 1.1.0

مرجع العمل: PROJECT_GUIDE.md. الفرع الوحيد المعتمد: main.
iOS: Codemagic، workflow NAYVO - iOS upload. الويب والسيرفر: Hostinger Docker.
معرّفات التطبيق ثابتة: app.replit.forsa وtoday.forsa.

## حالة الدمج
تم اعتماد الواجهات المشتركة الجديدة وإعدادات البناء.
هذا ليس إثباتاً على نجاح توقيع iOS أو الرفع أو ترحيل قاعدة بيانات الإنتاج.

## قرار المستخدم: حذف بيانات النموذج القديم قبل النشر
النموذج الجديد يستبدل campaigns/campaign_products بمنتجات وسحوبات،
ويغيّر orders وtickets وorder_items.
المستخدم طلب صراحة حذف البيانات القديمة بتاريخ 2026-09-22.
إعادة التهيئة مصرح بها؛ لا تفرض ترحيل الطلبات والتذاكر القديمة كشرط للنشر.
هذا التفويض يتعلق ببيانات التطبيق، ولا يشمل مفاتيح التوقيع أو أسرار الخدمة أو النسخ الاحتياطية.

قبل نشر الإنتاج:
1. خذ نسخة احتياطية وتحقق من استعادتها في قاعدة معزولة.
2. أوقف الكتابة أثناء إعادة التهيئة. حدّد قاعدة التطبيق الفعلية من إعدادات النشر،
   ثم نفّذ حذف البيانات القديمة المصرح به وتطبيق المخطط الجديد.
   لا تنفّذ أوامر حذف عامة على قواعد أو وحدات تخزين أخرى.
   جهّز حساب الإدارة واملأ المنتجات والسحوبات وطرق الدفع الفعلية بعد التهيئة.
3. عالج توافق التطبيقات المثبتة التي تستدعي /api/campaigns و/api/purchase
   و/api/cart-purchase؛ المسارات القديمة غير موجودة في النموذج الجديد.
4. جرّب الخادم والويب على بيئة مرحلية بقاعدة منفصلة وبيانات اختبار،
   ثم جرّب التصفح والسلة والدخول والدفع وتأكيد الطلب ومنح القسائم.
5. تحقق من المنتجات والصور والأسعار والسحب وطرق الدفع الفعلية في لوحة الإدارة.
   لا تستخدم بيانات الصور التوضيحية على أنها بيانات حقيقية.
6. بعد نجاح ذلك فقط: نشر السيرفر ثم الويب ثم تحديث التطبيقات.

## بناء iOS
اتبع IOS_CODEMAGIC.md، واختر main.
القيم المقترحة حالياً: release_version=1.1.0 وbuild_number=14.
تحقق من App Store Connect قبل كل رفع، وزد build_number عند الحاجة.
يلزم provisioning profile متوافق مع شهادة التوزيع وباقة app.replit.forsa.
رفع IPA لا ينشره تلقائياً للعامة؛ اختبره في TestFlight قبل إرساله للمراجعة.

## بناء الويب
npm ci
EXPO_PUBLIC_DOMAIN=nayvo.store npm run web:build
npm run server:build
ملفات app/ وcomponents/ نفسها تستخدم في الويب والتطبيق.
بعد النسخ الاحتياطي وإعادة التهيئة المصرح بها اتبع HOSTINGER_DEPLOYMENT.md.

## بعد النشر
تحقق من /api/health و/api/products و/api/draws/current،
ومن الطلب والدفع والقسائم والإشعارات على أجهزة حقيقية.


## أمر النشر الأول (حذف مصرح به)
من /opt/forsa بعد تحديث main:

```bash
bash scripts/deploy-production.sh --reset-data
```

هذا يمسح بيانات التطبيق في مخطط public من قاعدة forsa، بما فيها الحسابات والطلبات والتذاكر والمنتجات والسحوبات القديمة.
يعاد إنشاء admin بكلمة ADMIN_PASSWORD الموجودة في .env.production؛ بيانات الاتصال ومفاتيح التوقيع لا تتغير.
يحفظ السكربت نسخة قاعدة البيانات بصلاحيات خاصة في backups/، ويتحقق من أرشيفها قبل تنفيذ الحذف.
بناء الصورة يسبق إيقاف التطبيق. حذف المخطط وإنشاء 17 جدولاً يحدثان في معاملة واحدة.
بعد النجاح يضع علامة في .deploy/ تمنع تكرار المسح. في التحديثات التالية استخدم --update فقط.

## الويب بعد النشر
- / المتجر المشترك مع التطبيق، بعرض متجاوب للجوال والكمبيوتر.
- /products كتالوج المنتجات، و/shop يحوّل إليه للتوافق مع الروابط القديمة.
- /admin/login تسجيل دخول الإدارة، ثم /admin لوحة المنتجات والسحوبات الجديدة.
- /about الصفحة التعريفية السابقة.
بعد إعادة التهيئة سيكون الكتالوج فارغاً حتى إضافة المحتوى الحقيقي. لا تنشئ منتجات أو فائزين وهميين.

## إذا فشل النشر
السكربت يعرض المرحلة المتعثرة ومسار النسخة الاحتياطية. لا تكرر الحذف ولا تحذف علامة .deploy.
إذا فشل SQL فالمعاملة تتراجع تلقائياً؛ التطبيق يبقى متوقفاً حتى معالجة المشكلة.
احتفظ بصورة nayvo-rollback ذات نفس تاريخ النسخة الاحتياطية. لاستعادة الإنتاج، أوقف التطبيق،
واستعد الأرشيف إلى قاعدة التطبيق المعروفة باستخدام pg_restore مع تنظيف المخطط الجديد، ثم شغّل صورة الإصدار السابق.
لا تستخدم docker compose down -v: لا حاجة لحذف وحدات التخزين أو بيانات Caddy.

## التحقق المنفّذ أثناء التجهيز
نجحت 6 اختبارات لحدود النشر (فشل البناء، قاعدة خاطئة، فشل النسخ الاحتياطي، فشل SQL، منع تكرار المسح، والتحديث دون حذف).
تم تطبيق مخطط الجداول الـ17 واختبار التراجع عند فشل SQL باستخدام PGlite محلياً.
هذه ليست تجربة على قاعدة الإنتاج أو دليلاً على تنفيذ النشر الفعلي.

